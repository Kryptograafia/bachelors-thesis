OPENAI_API_KEY=
# Gemini — used when --llm google (default model: gemini-2.5-flash-lite)
GOOGLE_API_KEY=

# Embeddings for retrieval (RAG)
# Fully local via Ollama.
# EMBEDDINGS_MODEL=nomic-embed-text

# M6 guardrail (local via Ollama)
# GUARDRAIL_MODEL=granite3.2:8b
